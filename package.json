{
  "name": "react-gsap-aos-monorepo",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "dev": "pnpm -r run dev"
  }
}
